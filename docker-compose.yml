version: "3.9"
services:
  backend:
    build: ./backend
    ports:
      - "4000:4000"
  frontend:
    build: ./frontend
    environment:
      - NEXT_PUBLIC_API_BASE=http://backend:4000
    ports:
      - "3000:3000"
    depends_on:
      - backend
